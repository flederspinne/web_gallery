.masonry
    each val in image_arr
        if (val.type !== "Avatar")
            div(onclick="" class="img_item" id="img_item_" + val.id)
                .img_data_top
                    span(id="likes_count_" + val.id class="likes_count") #{val.likes.length}
                    // Если убрать одинарные кавычки из параметра, передаваемого в post_like, ничего работать не будет,
                        потому что id у нас строка!
                        Со сравнением id у нас всегда проблемы, если это mongoose'овский ObjectId.
                        Всегда приводить их к единому типу вручную перед сравнением!
                    if ((user) && (val.likes.indexOf((user._id).toString()) === -1) && ((user._id).toString() !== (val.author_id).toString()))
                        span(onclick="like('" + val.id + "', '" + val.author_id + "')" class="like_button") &#x2764;
                    else
                        span(class="like_button disabled") &#x2764;
                    if (user)
                        if ((user._id).toString() === (val.author_id).toString())
                            a(onclick="delete_img('" + val.id + "')" id="img_delete_" + val.id class="delete") &#x2716;

                img(src="/img/" + val.id class="img")

                .img_data_bottom
                    a(href="/author/" + val.author_id class="author_name") #{val.author_name}
                    br
                    each tag in val.tag
                        a(onclick="search_by_tag('" + tag + "')" class="tag")= tag
                        |   &#8194;



//
    Функция, отправляющая id фотки по какому-то URL через GET-запрос.
    Обработчик маршрута заново получает всю инфу (можно для солидности какую-то дополнительную,
    которой на миниатюре не видно), пакует в объект со страшным названием и отправляет обратно сюда.
    Здесь мы, если есть инфа внутри страшного названия, показываем новый div с картинкой,
    где отображена вся инфа, запакованная в объект со страшным названием.
    Когда закрываем увеличенную картинку, новый div detach'ится.


script.
    $(document).ready(function () {
        var img_data_bottom_height = $(".img_data_bottom").css("height");
        $(".img_data_bottom").css("margin-top", "-" + img_data_bottom_height);
    });

    $(".img_item").hover(function () {
        $("#" + $(this).prop("id") + " .img_data_top").css("visibility", "visible");
        $("#" + $(this).prop("id") + " .img_data_bottom").css("visibility", "visible");
    }, function () {
        $("#" + $(this).prop("id") + " .img_data_top").css("visibility", "hidden");
        $("#" + $(this).prop("id") + " .img_data_bottom").css("visibility", "hidden");
    });

