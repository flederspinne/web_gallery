extends layout

block content

    h1 Мой профиль
    p Логин: #{user.username}
    p Мой рейтинг: #{user.rating}
    if (subs_arr)
        p Мои подписки: &nbsp;
        each author in subs_arr
            a(href="/author/" + author.id) #{author.username}
            |   &#8194;